import createFacetDictionary from './create-facet-dictionary'
import getApiPath from './get-api-path'
import hasStoredSearch from './has-stored-search'
import session from './session'

export default {
  createFacetDictionary,
  getApiPath,
  hasStoredSearch,
  session,
}
